это web-api, сделанный на джанго, с использованием библиотеки djangorestframework
все библиотеки есть в виртуальном окружение
rest_framework
psycopg2
drf-yasg

1) в файле settings.py меняем бд на postgres, чтобы работать с postgres нужно установить в виртуальное окружение библиотеку psycopg2
   * настраиваем все как нужно/пользователя/порт/хост/пароль

2) в файле model.py делаем с помощью ооп модель данных, региструем нашу модель в файле admin.py. Создаем файл под названием serialazers  и делаем сериалайзер, 
чтобы получить json формат. Cоздаем view класс, где не указываем http метод, это означает,что мы можем пользоваться всеми методами

3) регистируем через router нашу view  функцию. запускаем сервер и переходим по адресу api/serials.
Видим json формат. Через postman проверяем чтобы работали http методы и обновлялись/удалялись

4) Создаем документацию в swagger с помощью библиотеки drf-yasg, читаем ее документацию и выполняем

5) адрес api/serials показывает данные в бд через json формат 
адрес /swagger показывает нашу http документацию